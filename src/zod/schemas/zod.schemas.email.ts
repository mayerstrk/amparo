import type { z } from "zod";

// const emailSchema = z.string().email().nonempty();
// const isEmail = (value: unknown): value is string => {
//   return emailSchema.safeParse(value).success;
// };
//
// export { emailSchema, isEmail };
